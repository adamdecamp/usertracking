import './user-guide.css';

const formats = [
  ['General System Access Request', 'Last_First_(ORG)_GEN_SAAR_DDMMMYYYY'],
  ['Privileged System Access Request', 'Last_First_(ORG)_PRIV_TYPE_SAAR_DDMMMYYYY'],
  ['DoD Cyber Awareness', 'Last_First_(ORG)_DoD_Cyber_Cert_DDMMMYYYY'],
  ['General Agreement', 'Last_First_(ORG)_GEN_User_Agreement_DDMMMYYYY'],
  ['General And Privileged Agreement', 'Last_First_(ORG)_GEN_and_PRIV_Agreement_DDMMMYYYY'],
  ['8140 Certification Memo', 'Last_First_(ORG)_8140_Cert_Memo_DDMMMYYYY'],
  ['Privileged User Training', 'Last_First_(ORG)_PRIV_Training_Cert_DDMMMYYYY'],
  ['DTA Training', 'Last_First_(ORG)_DTA_Training_Cert_DDMMMYYYY'],
  ['DTA Agreement', 'Last_First_(ORG)_DTA_Agreement_DDMMMYYYY'],
];

export default function Guide() {
  return <main className="guide">
    {/* Plain anchor keeps the same guide compatible with the portable Vite build. */}
    {/* eslint-disable-next-line @next/next/no-html-link-for-pages */}
    <a href="/">← Return to the Tracker</a>
    <header><p>Reference</p><h1>User Guide</h1></header>
    <nav>{['Getting Started','Systems','Users','Evidence','Sync & Backup','Exports & Notifications','Archive'].map(x=><a key={x} href={`#${x.toLowerCase().split(' ')[0]}`}>{x}</a>)}</nav>
    <section id="getting"><h2>1. Getting Started</h2><ol><li>Open the tracker in Chrome or Edge.</li><li>Add or select an information system.</li><li>Map that system&apos;s shared folder and approve folder access.</li><li>Click Sync and review the Current, Missing, and Overdue totals.</li><li>Click the Missing or Overdue total to filter affected users and prepare notifications.</li></ol><aside>Each information system has its own shared folder. On another computer, map each applicable system folder and click Sync. Folder access must be approved on each computer.</aside></section>
    <section id="systems"><h2>2. Manage Information Systems</h2><p>Use <b>Manage systems</b> to add a system, set its type and organization, map its shared folder, archive it, or unarchive it. Every system folder contains only that system&apos;s users, evidence, manifest, audit logs, and backups. Archiving locks the system and its users while preserving all records.</p></section>
    <section id="users"><h2>3. Add and Update Users</h2><p>Map the selected system&apos;s shared folder and choose <b>Add user</b> in the User Directory header. General, Privileged, and DTA users receive the appropriate evidence requirements.</p><h3>Upload and Store New-User Evidence</h3><ol><li>Enter the user&apos;s identity, organization, email, and role.</li><li>For a Privileged or DTA account, enter the full account name as <code>username_type</code>. The tracker derives the Privileged User Type from everything after the final underscore. Separate multiple accounts with commas—for example, <code>jsmith_admin, jsmith_dta</code>.</li><li>Upload the original document for every displayed evidence requirement. Do not upload an existing ZIP.</li><li>Choose <b>Add user</b>. The tracker compresses each document into its own ZIP before storing it.</li><li>The files are saved under <code>User Evidence / Organization / Last_First</code> inside that information system&apos;s mapped shared folder.</li></ol><aside>Each ZIP retains the original evidence filename, including its artifact keywords and date. Missing and Overdue validation therefore continues to work against the stored ZIP filename.</aside><p>The User Directory can be searched by privileged username and filtered by role and derived Privileged User Type. Privileged usernames and types also appear in CSV exports. The directory displays only document categories required by every user currently shown. Privileged users require the privileged agreement, 8140 memo, and Privileged User Training certificate. DTA users require DTA training and the DTA agreement. Open a User Record to replace evidence, disable or enable access, or modify privileges. Updated documents are compressed and stored, and newly added roles require their additional evidence before submission.</p></section>
    <section id="evidence"><h2>4. Filename Rules</h2><p>Use the formats below. The filename must begin with <b>Last_First</b> in that exact order: the first segment is the user&apos;s last name and the second segment is the user&apos;s first name. Do not use spaces in evidence filenames; use underscores between words and sections. All dates must use <b>DDMMMYYYY</b>, such as <b>24AUG2026</b>, with a two-digit day, three-letter English month abbreviation, and four-digit year. For a privileged SAAR, replace <code>_TYPE</code> with the privileged-account suffix—for example, <code>_admin</code>, <code>_DTA</code>, <code>_dev</code>, or <code>_cyber</code>. A complete example is <code>Smith_John_(LM)_PRIV_admin_SAAR_24AUG2026.pdf</code>. The first value in parentheses becomes the user&apos;s organization—for example, <b>(LM)</b> becomes LM, <b>(GOV)</b> becomes GOV, and <b>(Boeing)</b> becomes Boeing. The validator checks the ordered Last_First identity, the key artifact words, and a valid DDMMMYYYY date. Invalid or absent evidence is Missing; valid evidence older than one year is Overdue.</p><div className="formats">{formats.map(([label,format])=><div key={label}><span>{label}</span><code>{format}</code></div>)}</div></section>
    <section id="sync"><h2>5. Shared-Folder Sync, Audit Logs, and Backups</h2><p><b>Map system folder</b> authorizes the selected information system&apos;s evidence root. If that folder already contains a single-system tracker manifest, its system and users are loaded before anything is written. <b>Sync</b> reloads that system&apos;s manifest and scans every subfolder. The filename parser always treats the first underscore-separated segment as <b>Last Name</b> and the second as <b>First Name</b>.</p><p>For an existing user, Sync proposes matching artifact and organization updates. A valid SAAR for an identity not yet in the database creates a proposed new-user record instead. The Sync Review window displays separate editable Last Name, First Name, Organization, and optional Email fields, plus the role inferred from the SAAR. Verify or correct those fields and deselect any user or artifact that should not be applied.</p><p>Only approved matches and proposed users are written when you choose <b>Apply verified updates</b>. Evidence-like files that do not match a verified <code>Last_First</code> identity remain listed as unmatched. If selected files contain conflicting organization tags for one existing user, the app requires you to resolve the conflict before applying.</p><p>Only one operator can modify a mapped system folder at a time. The app maintains a lightweight session lease in the selected system folder. After 15 minutes without activity, the operator is disconnected and must choose <b>Yes, reconnect me</b>. Reconnection is allowed only when another operator is not active in that folder.</p><p>Consequential changes—including system administration, user and privilege changes, evidence storage, exports, and synchronization—belong in one daily text file in that system folder&apos;s <code>Audit Logs</code>. Viewing, searching, filtering, and theme changes are excluded.</p></section>
    <section id="exports"><h2>6. Exports and Notification Messages</h2><h3>Export CSV Results</h3><p>Filter by information system, organization, Privileged User Type, status, and one or more artifact types. The full Privileged Username and its derived Privileged User Type are included in every CSV row. Archived systems are labeled Active or Archived in every exported row.</p><h3>Prepare Missing or Overdue Notifications</h3><ol><li>Click the <b>Missing</b> or <b>Overdue</b> summary card on the main page. The selected status is applied automatically.</li><li>Select the information system and organization to contact.</li><li>Select exactly one artifact type. The recipient list immediately changes to users whose selected artifact has that status.</li><li>Review the recipient count and matched users. Disabled users and users in archived systems are excluded.</li><li>Use <b>Copy scripted message</b> for an individual notice, or <b>Copy BCC list</b> to copy every matched address.</li><li>Choose <b>Open Outlook Email</b> to create a draft with matched addresses in BCC and a subject and message naming the selected artifact.</li><li>Review the Outlook draft, make any required organizational edits, and send it manually.</li></ol><aside>The tracker never sends email automatically. Outlook must be configured as the computer&apos;s email handler for the draft button to open it.</aside></section>
    <section id="archive"><h2>7. Review Archived Records</h2><p>Archived systems and users remain available as locked, read-only history. Unarchive the system to restore normal editing.</p></section>
    <footer>This application supports NIST SP 800-53-aligned recordkeeping. Formal compliance depends on organizational policies, deployment, access controls, and assessment.</footer>
  </main>;
}

