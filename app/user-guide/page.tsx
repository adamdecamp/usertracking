import './user-guide.css';

const formats = [
  ['General System Access Request', 'Last_First_(ORG)_GEN_SAAR_DDMMMYYYY'],
  ['Privileged System Access Request', 'Last_First_(ORG)_PRIV_TYPE_SAAR_DDMMMYYYY'],
  ['DoD Cyber Awareness', 'Last_First_(ORG)_DoD_Cyber_Cert_DDMMMYYYY'],
  ['General Agreement', 'Last_First_(ORG)_GEN_User_Agreement_DDMMMYYYY'],
  ['General And Privileged Agreement', 'Last_First_(ORG)_GEN_and_PRIV_Agreement_DDMMMYYYY'],
  ['8140 Certification Memo', 'Last_First_(ORG)_8140_Cert_Memo_DDMMMYYYY'],
  ['Privileged User Training', 'Last_First_(ORG)_PRIV_Training_Cert_DDMMMYYYY'],
  ['DTA Training', 'Last_First_(ORG)_DTA_Training_Cert_DDMMMYYYY'],
  ['DTA Agreement', 'Last_First_(ORG)_DTA_Agreement_DDMMMYYYY'],
];

export default function Guide() {
  return <main className="guide">
    {/* Plain anchor keeps the same guide compatible with the portable Vite build. */}
    {/* eslint-disable-next-line @next/next/no-html-link-for-pages */}
    <a href="/">← Return to the Tracker</a>
    <header><p>Reference</p><h1>User Guide</h1></header>
    <nav>{['Getting Started','Systems','Users','Evidence','Sync & Backup','Exports & Notifications','Archive'].map(x=><a key={x} href={`#${x.toLowerCase().split(' ')[0]}`}>{x}</a>)}</nav>
    <section id="getting"><h2>1. Getting Started</h2><ol><li>Open the tracker in Chrome or Edge.</li><li>Add a new information system, or choose <b>Map System Folder</b> to load an existing one.</li><li>Map that system&apos;s shared folder and approve folder access.</li><li>Click Sync and review the Current, Missing, and Overdue totals.</li><li>Click the Missing or Overdue total to filter affected users and prepare notifications.</li></ol><aside>Every release opens without systems or users. Operational records are loaded only from each mapped information-system folder and are not retained in browser storage. Folder access must be approved on each computer.</aside></section>
    <section id="systems"><h2>2. Manage Information Systems</h2><p>Use <b>Manage Systems</b> to add a system, set its type and organization, map its shared folder, archive it, or unarchive it. Every system folder contains only that system&apos;s users, evidence, manifest, audit logs, and backups. Archiving locks the system and its users while preserving all records.</p></section>
    <section id="users"><h2>3. Add and Update Users</h2><p>Map the selected system&apos;s shared folder and choose <b>Add User</b> in the User Directory header. <b>General</b> and <b>Privileged</b> are the only user roles. DTA is a Privileged User Type, not a separate role.</p><h3>Upload and Store New-User Evidence</h3><ol><li>Enter the user&apos;s identity, organization, email, and select General or Privileged.</li><li>For a Privileged account, enter the full account name as <code>username_type</code>. The tracker derives the Privileged User Type from everything after the final underscore. Separate multiple accounts with commas—for example, <code>jsmith_admin, jsmith_dta</code>. A <code>_dta</code> suffix keeps the user&apos;s role as Privileged and adds DTA as a Privileged User Type.</li><li>Upload either a readable PDF or a ZIP containing exactly one readable PDF for every displayed evidence requirement. Other file types, mixed-content ZIPs, and multi-document ZIPs are rejected.</li><li>Choose <b>Add User</b>. A direct PDF is compressed into its own ZIP before storage; an already valid one-PDF ZIP is stored without another compression layer.</li><li>The files are saved under <code>User Evidence / Organization / Last_First</code> inside that information system&apos;s mapped shared folder.</li></ol><aside>Each ZIP retains the evidence filename, including its artifact keywords and date. Missing and Overdue validation therefore continues to work against the stored ZIP filename.</aside><p>The User Directory can be searched by privileged username and filtered by General or Privileged role and by derived Privileged User Type. The directory displays Privileged User Type but keeps the full privileged username inside the User Record and CSV exports. The directory displays only document categories required by every user currently shown. All Privileged users require the privileged agreement, 8140 memo, and Privileged User Training certificate. Privileged users with DTA type additionally require DTA training and the DTA agreement. Open a User Record to replace evidence, disable or enable access, or modify privileges. Updated PDF evidence is validated and stored as a ZIP, and newly added roles or Privileged User Types require their additional evidence before submission.</p></section>
    <section id="evidence"><h2>4. Filename Rules</h2><p>Use the formats below. The filename must begin with the user&apos;s <b>Last Name</b> followed by <b>First Name</b> in that order. Underscores remain the recommended separator, but Sync also accepts a comma or ordinary spaces between Last and First, missing underscores between the other sections, and additional spaces. For example, both <code>Brown_Jacob_(LM)_DoD_Cyber_Cert_26AUG2026.pdf</code> and <code>Brown, Jacob (LM) DoD Cyber Cert 26AUG2026.pdf</code> are accepted. All dates must use <b>DDMMMYYYY</b>, such as <b>24AUG2026</b>, with a two-digit day, three-letter English month abbreviation, and four-digit year. For a privileged SAAR, replace <code>_TYPE</code> with the privileged-account suffix—for example, <code>_admin</code>, <code>_DTA</code>, <code>_dev</code>, or <code>_cyber</code>. A complete canonical example is <code>Smith_John_(LM)_PRIV_admin_SAAR_24AUG2026.pdf</code>. The first value in parentheses becomes the user&apos;s organization—for example, <b>(LM)</b> becomes LM, <b>(GOV)</b> becomes GOV, and <b>(Boeing)</b> becomes Boeing. The validator checks the ordered Last-First identity, required artifact key words, and a valid DDMMMYYYY date without requiring one exact separator style. A DoD Cyber certificate must contain the separate <code>DoD</code> token; <code>_cyber</code> in a privileged SAAR is only an account type and does not count as the certificate. A SAAR is either Current or Missing and never becomes Overdue. Other valid evidence becomes Overdue after one year.</p><div className="formats">{formats.map(([label,format])=><div key={label}><span>{label}</span><code>{format}</code></div>)}</div></section>
    <section id="sync"><h2>5. Shared-Folder Sync, Audit Logs, and Backups</h2><p><b>Map System Folder</b> opens the native Windows folder selector in the foreground and authorizes the selected information system&apos;s evidence root. If that folder already contains a single-system tracker manifest, its system and users are loaded before anything is written. <b>Sync</b> reloads that system&apos;s manifest and scans every subfolder. The filename parser always treats the first underscore-separated segment as <b>Last Name</b> and the second as <b>First Name</b>. When multiple valid files match the same user and artifact type, the tracker uses the file with the newest <b>DDMMMYYYY</b> filename date for compliance status, display, notifications, and exports.</p><p>Sync accepts direct PDFs and ZIPs containing exactly one PDF. It checks actual PDF and ZIP structure instead of trusting the extension. Renamed non-PDFs, unreadable PDFs, unsafe archive paths, encrypted or unsupported ZIPs, mixed-content archives, multi-document archives, files over 100 MB, and suspiciously high ZIP expansion ratios are rejected. Rejected evidence-like filenames appear in the Sync Review with the reason and are never used to populate compliance status.</p><p>Sync only identifies duplicate, superseded, and loose PDF evidence; it never changes files. When qualifying files are found, Sync displays a <b>Clean Up</b> button. Choose it to review files individually or use <b>Select All</b>. Duplicate and superseded files explicitly approved by the operator are moved into <code>Archive Review / YYYY-MM-DD</code> for later manual review or deletion. Selected loose PDFs are compressed in place. The new ZIP is reopened and validated as containing exactly one readable PDF before the original PDF is deleted; a creation, validation, or deletion failure leaves no incomplete replacement and preserves the original PDF. Archived review files and generated reports are excluded from later Sync scans.</p><p>For an existing user, Sync proposes matching artifact and organization updates. A valid SAAR for an identity not yet in the database creates a proposed new-user record instead. Sync reads the official email from the fillable PDF field <b>4 OFFICIAL EMAIL ADDRESS</b>. Scanned copies and non-PDF SAARs are not accepted for new-user discovery. If Section 4 is empty, invalid, or unavailable, the proposed user stays blocked until the corrected fillable SAAR is placed in the shared folder and Sync is run again.</p><p>The Sync Review window displays separate editable Last Name, First Name, and Organization fields, the read-only email extracted from Section 4, and the role inferred from the SAAR. A <code>PRIV</code> SAAR creates a Privileged role. Its editable Privileged Account Type field is prepopulated with the <code>_TYPE</code> suffix, such as <code>_admin</code> or <code>_DTA</code>; DTA remains a Privileged User Type. Verify or correct the permitted fields and deselect any user or artifact that should not be applied. Only approved matches and proposed users are written when you choose <b>Apply Verified Updates</b>.</p><p>A correctly named supporting artifact cannot create a user by itself. If its <code>Last_First</code> identity does not exist in the selected system, Sync lists it as unmatched and names the missing user identity. Add that user manually or include the user&apos;s completed fillable SAAR before syncing again. If selected files contain conflicting organization tags for one existing user, the app requires you to resolve the conflict before applying.</p><p>In the portable Windows app, the launcher—not the browser tab—performs the critical file operations. It atomically writes the manifest, daily CSV, timestamped JSON snapshots, SHA-256 files, evidence ZIPs, audit entries, reports, and session lease. The browser validates changes and sends them to this local-only storage service. This design keeps the final disk write and shutdown backup available even if the browser window closes unexpectedly.</p><p>The storage-health strip shows <b>Last Saved</b>, <b>Last Backup</b>, and <b>Last Sync</b> for the selected system. Green means the manifest and backup are current; amber means a save or verification is in progress; red means the mapped folder needs attention. Choose <b>Verify Backup</b> to validate the current manifest, newest JSON snapshot, and complete audit hash chain. A verification failure does not delete or overwrite evidence.</p><p>Only one portable Windows launcher can modify a mapped system folder at a time. While the system is active, the launcher holds <code>tracker-exclusive-session.lock</code> open with an exclusive Windows file lock. A second computer is denied write access until the first operator switches systems, disconnects, closes the app, or stops renewing the lock for three minutes. The file records the operator, computer, process, session, and update time for troubleshooting. A leftover lock file after a crash is harmless because ownership depends on the open Windows handle, not the file&apos;s presence. This cross-computer protection requires a Windows or SMB shared folder that honors native file locking; browser-only development mode remains advisory.</p><p>After 15 minutes without activity, the operator is disconnected and must choose <b>Yes, Reconnect Me</b>. Reconnection succeeds only when another operator does not hold the exclusive lock.</p><p>Each meaningful database state creates a full-fidelity, timestamped JSON snapshot in the selected system&apos;s <code>backup</code> folder, alongside the daily CSV report. Every JSON snapshot has a matching <code>.sha256</code> file and includes the system, users, artifact references, and administrative change histories. Unchanged data is not duplicated, at least one snapshot is retained for each active day, and the newest 30 snapshots are kept.</p><p>Use <b>Restore Backup</b> to review available snapshots. The launcher verifies both the SHA-256 file and the snapshot&apos;s internal content hash before enabling restoration, verifies the selected file again immediately before use, and backs up the current database before replacing its records. Evidence files are never deleted by a database restore.</p><p>The portable Windows app shuts down after 60 minutes without keyboard, pointer, or touch activity, and it also shuts down when its browser window is closed. Before either automatic shutdown, the launcher verifies or refreshes the mapped systems&apos; JSON backups. The browser also refreshes the selected system&apos;s manifest and daily CSV, records the shutdown in the audit log, and releases the shared-folder session when it remains available. If a pending write or final backup fails, shutdown is held so the failure can be corrected without discarding changes.</p><p>Consequential changes—including system administration, user and privilege changes, evidence storage, exports, report generation, and synchronization—are written to one UTF-8 JSON Lines text file per UTC day in that system&apos;s <code>Audit Logs</code> folder. Every entry contains an ISO 8601 UTC timestamp, the Windows operator, a continuous sequence, the previous entry&apos;s SHA-256, and its own SHA-256. The chain continues across daily files. Before appending, and whenever storage is verified, the launcher checks the entire chain and refuses to extend it if an entry was changed, removed, reordered, or inserted. Existing legacy <code>audit-*.txt</code> files remain available but are not part of the cryptographic chain. Viewing, searching, filtering, and theme changes are excluded.</p></section>
    <section id="exports"><h2>6. Reports, Exports, and Notification Messages</h2><h3>Compliance Snapshot</h3><p>Choose <b>Compliance Snapshot</b>, select one or more mapped systems and organizations, choose All, Active, or Disabled user records, and set the reporting date. The PDF contains its report ID, UTC generation time, Windows operator, application and rule-set versions, scope, user and requirement totals, overdue aging, and breakdowns by organization, information system, role, Privileged User Type, and artifact.</p><p>The PDF downloads to the computer and a matching copy is written to each selected system&apos;s <code>Reports</code> folder with a <code>.sha256</code> file. Report ID, scope, filename, and hash are recorded in each system&apos;s audit chain. The report is administrative evidence and does not independently establish control effectiveness.</p><h3>Export CSV Results</h3><p>Filter by information system, organization, Privileged User Type, status, and one or more artifact types. The full Privileged Username and its derived Privileged User Type are included in every CSV row. Archived systems are labeled Active or Archived in every exported row.</p><h3>Prepare Missing or Overdue Notifications</h3><ol><li>Click the <b>Missing</b> or <b>Overdue</b> summary card on the main page. The selected status is applied automatically.</li><li>Select the information system and organization to contact.</li><li>Select exactly one artifact type. The recipient list immediately changes to users whose selected artifact has that status. SAAR is available for Missing notifications but is not listed for Overdue notifications because SAARs do not expire.</li><li>Review the recipient count and matched users. Disabled users and users in archived systems are excluded.</li><li>Use <b>Copy BCC List</b> if you need the matched addresses separately.</li><li>Choose <b>Open Outlook Email</b> to create one draft with every matched address in BCC and the approved Missing or Overdue message naming the selected artifact.</li><li>Review the Outlook draft, make any required organizational edits, and send it manually.</li></ol><aside>The tracker never sends email automatically. Outlook must be configured as the computer&apos;s email handler for the draft button to open it.</aside></section>
    <section id="archive"><h2>7. Review Archived Records</h2><p>Archived systems and users remain available as locked, read-only history. Unarchive the system to restore normal editing.</p></section>
    <footer>This application supports NIST SP 800-53-aligned recordkeeping. Formal compliance depends on organizational policies, deployment, access controls, and assessment.</footer>
  </main>;
}
