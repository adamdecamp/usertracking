import './user-guide.css';

const formats = [
  ['General System Access Request', 'Last_First_(ORG)_GEN_SAAR_DDMMMYYYY'],
  ['Privileged System Access Request', 'Last_First_(ORG)_PRIV_TYPE_SAAR_DDMMMYYYY'],
  ['DoD Cyber Awareness', 'Last_First_(ORG)_DoD_Cyber_Cert_DDMMMYYYY'],
  ['General Agreement', 'Last_First_(ORG)_GEN_User_Agreement_DDMMMYYYY'],
  ['General And Privileged Agreement', 'Last_First_(ORG)_GEN_and_PRIV_Agreement_DDMMMYYYY'],
  ['8140 Certification Memo', 'Last_First_(ORG)_8140_Cert_Memo_DDMMMYYYY'],
  ['Privileged User Training', 'Last_First_(ORG)_PRIV_Training_Cert_DDMMMYYYY'],
  ['DTA Training', 'Last_First_(ORG)_DTA_Training_Cert_DDMMMYYYY'],
  ['DTA Agreement', 'Last_First_(ORG)_DTA_Agreement_DDMMMYYYY'],
];

export default function Guide() {
  return <main className="guide">
    {/* Plain anchor keeps the same guide compatible with the portable Vite build. */}
    {/* eslint-disable-next-line @next/next/no-html-link-for-pages */}
    <a href="/">← Return to the Tracker</a>
    <header><p>Reference</p><h1>User Guide</h1></header>
    <nav>{['Getting Started','Systems','Users','Evidence','Sync & Backup','Exports & Notifications','Archive'].map(x=><a key={x} href={`#${x.toLowerCase().split(' ')[0]}`}>{x}</a>)}</nav>
    <aside>Every validation build and signed public release includes a freshly generated, one-page <b>Executive Capability Summary</b> PDF beside the Windows executable and checksum file. It describes mission value, operational capabilities, audit readiness, secure deployment, and the tool&apos;s administrative scope.</aside>
    <section id="getting"><h2>1. Getting Started</h2><ol><li>Open the tracker in Chrome or Edge.</li><li>Add a new information system, or choose <b>Map System Folder</b> to load an existing one.</li><li>Map that system&apos;s shared folder and approve folder access. Sync starts automatically after mapping.</li><li>Review Sync Review and the Current, Missing, and Overdue totals. On later portable-app launches, the last active mapped system is restored and synced automatically. Selecting another mapped system also syncs that system automatically.</li><li>Choose <b>Stop Sync</b> while a scan is running if you need to cancel it. Cancelled scan results are discarded and no partial database update is applied.</li><li>You may close Sync Review or apply verified updates without running cleanup. Deferred and failed actions remain available under the correct system&apos;s main <b>Clean Up</b> button until completed or replaced by a later Sync of that system.</li><li>Click the Missing or Overdue total to filter affected users and prepare notifications.</li><li>Choose <b>View Audit Log</b> to verify and review the selected system&apos;s read-only audit entries.</li><li>When finished, choose <b>Log Off</b>. The tracker completes its final backup, records the logoff, releases the shared-folder lock, and terminates the portable Windows application.</li></ol><aside>Folder Sync is the primary way to create and update records. Manual Add User remains available only as a fallback. Every release opens without bundled systems or users. The portable launcher remembers only each mapped folder path in the signed-in Windows user&apos;s Local AppData. On the next launch it revalidates those paths, reloads operational records from the shared folders, reacquires the selected system&apos;s lock, and starts Sync. User records are not copied into browser storage or the mapping cache.</aside></section>
    <section id="systems"><h2>2. Manage Information Systems</h2><p>Use <b>Manage Systems</b> to add a system, set its type and organization, map its shared folder, archive it, or unarchive it. Every system folder contains only that system&apos;s users, evidence, manifest, audit logs, and backups. The portable app remembers valid mappings for the current Windows account, so normally each folder is selected only once per computer. Remap a system if its shared path changes or becomes unavailable. Archiving locks the system and its users while preserving all records.</p></section>
    <section id="users"><h2>3. Add and Update Users</h2><p>Map the selected system&apos;s shared folder and choose <b>Add User</b> in the User Directory header. <b>General</b> and <b>Privileged</b> are the only user roles. DTA is a Privileged User Type, not a separate role.</p><h3>Upload and Store New-User Evidence</h3><ol><li>Start by dragging the completed SAAR onto its upload area or choose <b>Browse Files</b> to select it in Windows File Explorer. The app reads Last Name, First Name, and Organization from the filename first. When those values are unavailable or still use template placeholders, it reads them from supported fillable DD2875 XFA or derived-SAAR AcroForm fields instead. Middle Initial and Official Email are read from the fillable form when available.</li><li>Review and correct the prepopulated identity, organization, Official Email, and role. For manual Add User only, a valid Official Email can be entered when that field in the fillable SAAR is blank or invalid. Scanned or unreadable SAARs remain prohibited.</li><li>For a Privileged account, enter the full account name as <code>username_type</code>. The tracker derives the Privileged User Type from everything after the final underscore. Separate multiple accounts with commas—for example, <code>jsmith_admin, jsmith_dta</code>. A <code>_dta</code> suffix keeps the user&apos;s role as Privileged and adds DTA as a Privileged User Type.</li><li>Drag and drop or browse for either a readable PDF or a ZIP containing exactly one readable PDF for every displayed evidence requirement. Other file types, mixed-content ZIPs, and multi-document ZIPs are rejected.</li><li>Choose <b>Add User</b>. A direct PDF is compressed into its own ZIP before storage; an already valid one-PDF ZIP is stored without another compression layer.</li><li>The files are saved under <code>User Evidence / Organization / Last_First</code> inside that information system&apos;s mapped shared folder.</li></ol><aside>Each ZIP retains the evidence filename, including its artifact keywords and date. Missing and Overdue validation therefore continues to work against the stored ZIP filename.</aside><p>The User Directory can be searched by privileged username and filtered by General or Privileged role, derived Privileged User Type, organization, artifact, and compliance status. Choose <b>Reset Filters</b> to clear the search and restore every directory filter to its default. The directory displays Privileged User Type but keeps the full privileged username inside the User Record and CSV exports. The directory displays only document categories required by every user currently shown. All Privileged users require the privileged agreement, 8140 memo, and Privileged User Training certificate. Privileged users with DTA type additionally require DTA training and the DTA agreement. Open a User Record to replace evidence, disable or enable access, or modify privileges. Updated PDF evidence is validated and stored as a ZIP, and newly added roles or Privileged User Types require their additional evidence before submission.</p></section>
    <section id="evidence"><h2>4. Filename Rules</h2><p>Use the formats below. The filename must begin with the user&apos;s <b>Last Name</b> followed by <b>First Name</b> in that order. Underscores remain the recommended separator, but Sync also accepts a comma or ordinary spaces between Last and First, missing underscores between the other sections, and additional spaces. For example, both <code>Brown_Jacob_(LM)_DoD_Cyber_Cert_26AUG2026.pdf</code> and <code>Brown, Jacob (LM) DoD Cyber Cert 26AUG2026.pdf</code> are accepted. The standard date is <b>DDMMMYYYY</b>, such as <b>24AUG2026</b>. Sync also recognizes common valid alternatives, including <code>YYYYMMDD</code>, <code>MMDDYYYY</code>, <code>MMMDDYYYY</code>, <code>MMDDYY</code>, and separated numeric or month-name dates. It converts a recognized nonstandard date in an active evidence filename to DDMMMYYYY before matching. When an all-numeric date could be read more than one way, Sync uses U.S. month-day order; impossible dates remain invalid and are never guessed. For a privileged SAAR, replace <code>_TYPE</code> with the privileged-account suffix—for example, <code>_admin</code>, <code>_DTA</code>, <code>_dev</code>, or <code>_cyber</code>. A complete canonical example is <code>Smith_John_(LM)_PRIV_admin_SAAR_24AUG2026.pdf</code>. The first value in parentheses becomes the user&apos;s organization—for example, <b>(LM)</b> becomes LM, <b>(GOV)</b> becomes GOV, and <b>(Boeing)</b> becomes Boeing. The validator checks the ordered Last-First identity, required artifact key words, and a valid calendar date without requiring one exact separator style. A DoD Cyber certificate must contain the separate <code>DoD</code> token; <code>_cyber</code> in a privileged SAAR is only an account type and does not count as the certificate. A SAAR is either Current or Missing and never becomes Overdue. Other valid evidence becomes Overdue after one year.</p><div className="formats">{formats.map(([label,format])=><div key={label}><span>{label}</span><code>{format}</code></div>)}</div></section>
    <section id="sync"><h2>5. Shared-Folder Sync, Audit Logs, and Backups</h2><p><b>Map System Folder</b> opens the native Windows folder selector in the foreground and authorizes the selected information system&apos;s evidence root. If that folder already contains a single-system tracker manifest, its system and users are loaded before anything is written. <b>Sync</b> reloads that system&apos;s manifest and scans every subfolder. The filename parser treats the first recognized identity value as <b>Last Name</b> and the second as <b>First Name</b>, accepting underscores, a comma, ordinary spaces, and additional surrounding spaces. When multiple valid files match the same user and artifact type, the tracker uses the file with the newest normalized filename date for compliance status, display, notifications, and exports.</p><p>The <b>Processing Status</b> panel appears during Sync, verified clean-up actions, backup restoration, storage verification, and Compliance Snapshot generation. It displays the current phase, processed count, exact percentage when a total is known, and the final result. Initial folder discovery shows an indeterminate progress bar until the launcher knows how many files are present.</p><p>Sync accepts direct PDFs and ZIPs containing exactly one PDF. It checks actual PDF and ZIP structure instead of trusting the extension. Renamed non-PDFs, unreadable PDFs, unsafe archive paths, encrypted or unsupported ZIPs, mixed-content archives, multi-document archives, files over 100 MB, and suspiciously high ZIP expansion ratios are rejected. Scanned or flattened SAAR copies are not accepted for automatic user discovery because the required form fields cannot be verified. Rejected evidence-like filenames appear in the Sync Review with the reason and are never used to populate compliance status.</p><p>Sync automatically renames a recognized nonstandard evidence date to DDMMMYYYY in the same active folder and records the change in the audit log. Other correction, duplicate, superseded, and loose-PDF actions remain operator controlled. When the first Sync discovers valid users, choosing <b>Apply Verified Updates</b> ingests those records and immediately re-evaluates the evidence already scanned against the new User Directory. Any newly eligible duplicate, superseded, Rework, or loose-PDF actions open directly in Clean Up, so a second Sync is not required. Review files individually or use <b>Select All</b>. PDFs that require correction can be moved into the root <code>Rework</code> folder. Duplicate and superseded files explicitly approved by the operator are moved into <code>Archive Review / YYYY-MM-DD</code> for later manual review or deletion. Only loose PDFs matched to a verified User Directory record are eligible for compression. The new ZIP is reopened and validated as containing exactly one readable PDF before the original PDF is deleted; a creation, validation, or deletion failure leaves no incomplete replacement and preserves the original PDF. Rework, archived review files, and generated reports are excluded from later Sync scans.</p><p>For an existing user, Sync proposes matching artifact and organization updates. A valid SAAR for an identity not yet in the database creates a proposed new-user record instead. The filename is always the primary source for Last Name, First Name, and Organization. If those filename values are missing or still contain template placeholders, Sync reads the requester name, organization, and Official Email from either a standard DD Form 2875 XFA dataset or the derived SAAR&apos;s AcroForm fields. Filename role, privileged type, SAAR marker, and valid date are still required. A SAAR that fails any admission check is never shown as an addable user and is listed only for movement to <code>Rework</code>. Return a corrected fillable SAAR to an active evidence folder before running Sync again.</p><p>The Sync Review window displays separate editable Last Name, First Name, and Organization fields, the read-only email extracted from the form&apos;s Official Email field, and the role inferred from the filename. A <code>PRIV</code> SAAR creates a Privileged role. Its editable Privileged Account Type field is prepopulated with the <code>_TYPE</code> suffix, such as <code>_admin</code> or <code>_DTA</code>; DTA remains a Privileged User Type. Verify or correct the permitted fields and deselect any user or artifact that should not be applied. Only approved matches and proposed users are written when you choose <b>Apply Verified Updates</b>. In the manual <b>Add User</b> workflow only, the operator may enter a valid <b>Official Email</b> when the uploaded SAAR&apos;s email field is blank; Sync never substitutes a manually entered value.</p><p>A correctly named supporting artifact cannot create a user by itself. If its <code>Last_First</code> identity does not exist in the selected system, Sync lists it as unmatched and names the missing user identity. It is not offered for compression. Add that user manually or include the user&apos;s completed fillable SAAR before syncing again. If selected files contain conflicting organization tags for one existing user, the app requires you to resolve the conflict before applying.</p><p>In the portable Windows app, the launcher—not the browser tab—performs the critical file operations. It atomically writes the manifest, daily CSV, timestamped JSON snapshots, SHA-256 files, evidence ZIPs, audit entries, reports, and session lease. The browser validates changes and sends them to this local-only storage service. This design keeps the final disk write and shutdown backup available even if the browser window closes unexpectedly.</p><p>The storage-health strip shows <b>Last Saved</b>, <b>Last Backup</b>, and <b>Last Sync</b> for the selected system. Green means the manifest and backup are current; amber means a save or verification is in progress; red means the mapped folder needs attention. Choose <b>Verify Backup</b> to validate the current manifest, newest JSON snapshot, and complete audit hash chain. A verification failure does not delete or overwrite evidence.</p><p>Only one portable Windows launcher can modify a mapped system folder at a time. While the system is active, the launcher holds <code>tracker-exclusive-session.lock</code> open with an exclusive Windows file lock. A second computer is denied write access until the first operator switches systems, disconnects, closes the app, or stops renewing the lock for three minutes. The file records the operator, computer, process, session, and update time for troubleshooting. A leftover lock file after a crash is harmless because ownership depends on the open Windows handle, not the file&apos;s presence. This cross-computer protection requires a Windows or SMB shared folder that honors native file locking; browser-only development mode remains advisory.</p><p>After 15 minutes without activity, the operator is disconnected and must choose <b>Yes, Reconnect Me</b>. Reconnection succeeds only when another operator does not hold the exclusive lock.</p><p>Each meaningful database state creates a full-fidelity, timestamped JSON snapshot in the selected system&apos;s <code>backup</code> folder, alongside the daily CSV report. Every JSON snapshot has a matching <code>.sha256</code> file and includes the system, users, artifact references, and administrative change histories. Unchanged data is not duplicated, at least one snapshot is retained for each active day, and the newest 30 snapshots are kept.</p><p>Use <b>Restore Backup</b> to review available snapshots. The launcher verifies both the SHA-256 file and the snapshot&apos;s internal content hash before enabling restoration, verifies the selected file again immediately before use, and backs up the current database before replacing its records. Evidence files are never deleted by a database restore.</p><p>The portable Windows app shuts down after 60 minutes without keyboard, pointer, or touch activity, and it also shuts down when its browser window is closed. Before either automatic shutdown, the launcher verifies or refreshes the mapped systems&apos; JSON backups. The browser also refreshes the selected system&apos;s manifest and daily CSV, records the shutdown in the audit log, and releases the shared-folder session when it remains available. If a pending write or final backup fails, shutdown is held so the failure can be corrected without discarding changes.</p><p>Consequential changes—including system administration, user and privilege changes, evidence storage, exports, report generation, filename-date normalization, and synchronization—are written to one UTF-8 JSON Lines text file per UTC day in that system&apos;s <code>Audit Logs</code> folder. Every entry contains an ISO 8601 UTC timestamp, the Windows operator, a continuous sequence, the previous entry&apos;s SHA-256, and its own SHA-256. The chain continues across daily files. Before appending, and whenever storage is verified, the launcher checks the entire chain and refuses to extend it if an entry was changed, removed, reordered, or inserted. Existing legacy <code>audit-*.txt</code> files remain available but are not part of the cryptographic chain. Viewing, searching, filtering, and theme changes are excluded.</p></section>
    <section id="exports"><h2>6. Reports, Exports, and Notification Messages</h2><h3>Compliance Snapshot</h3><p>Choose <b>Compliance Snapshot</b>, select one or more mapped systems and organizations, and set the reporting date. Disabled users are always excluded. The PDF contains its report ID, UTC generation time, Windows operator, application and rule-set versions, scope, active-user and requirement totals, overdue aging, and breakdowns by organization, information system, role, Privileged User Type, and artifact.</p><p>The PDF downloads to the computer and a matching copy is written to each selected system&apos;s <code>Reports</code> folder with a <code>.sha256</code> file. Report ID, scope, filename, and hash are recorded in each system&apos;s audit chain. The report is administrative evidence and does not independently establish control effectiveness.</p><h3>Export Filtered User Directory Results</h3><p>Set the Information System, search text, Organization, Role, Privileged User Type, Artifact, and Compliance Status filters in the <b>User Directory</b>, then choose <b>Export CSV</b> beside <b>Add User</b>. The CSV contains only the users and applicable requirement rows that match the current directory filters, including exact Missing, Current, Due Within 30 Days, and Overdue status details. Choose <b>Reset Filters</b> to return to the full directory before exporting all results for the selected system.</p><h3>Prepare Missing, Due-Soon, or Overdue Notifications</h3><ol><li>Click the <b>Missing</b>, <b>Due Within 30 Days</b>, or <b>Overdue</b> summary card on the main page. The selected status is applied automatically.</li><li>Select the information system and organization to contact.</li><li>Select exactly one artifact type. The recipient list immediately changes to users whose selected artifact has that status. SAAR is available for Missing notifications but is not listed for Due Within 30 Days or Overdue notifications because SAARs do not expire.</li><li>Review the recipient count and matched users. Disabled users and users in archived systems are excluded.</li><li>Choose <b>Open Outlook Email</b> to create one draft with every matched address in BCC and the approved Missing, Due Within 30 Days, or Overdue message naming the selected artifact.</li><li>Review the Outlook draft, make any required organizational edits, and send it manually.</li></ol><aside>The tracker never sends email automatically. Outlook must be configured as the computer&apos;s email handler for the draft button to open it.</aside></section>
    <section id="archive"><h2>7. Review Archived Records</h2><p>Archived systems and users remain available as locked, read-only history. Unarchive the system to restore normal editing.</p></section>
    <aside><b>Incremental Sync:</b> After successful validation, the tracker stores a checksum-protected <code>tracker-sync-index.json</code> in each mapped system folder. Later Syncs still enumerate active folders to detect additions, moves, and deletions, but skip reopening evidence when its path, name, size, last-modified UTC value, and rule-set version are unchanged. The shared index accelerates another computer using the same mapped folder. New or changed files always receive full PDF or ZIP validation. Choose <b>Full Rescan</b> to ignore the index and validate every active evidence file; a missing, damaged, or outdated index also causes safe full validation automatically. Processing Status reports unchanged evidence skipped, new or changed evidence validated, and non-evidence files ignored.</aside>
    <aside><b>Long Sync Sessions:</b> While Sync is active, the tracker suspends the browser-session idle disconnect and keeps the portable Windows launcher active. The idle clocks restart from zero only after Sync completes, fails, or is stopped by the operator, so a large directory scan cannot log off the operator merely because it exceeds an idle limit.</aside>
    <footer>This application supports NIST SP 800-53-aligned recordkeeping. Formal compliance depends on organizational policies, deployment, access controls, and assessment.</footer>
  </main>;
}
